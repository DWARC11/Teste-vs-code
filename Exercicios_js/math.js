const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2); // Math.pow é uma função que calcula potencia/Power então é o mesmo que raio elevado ao quadrado

console.log(area);
console.log(typeof Math);

console.log(area.toFixed(2));//toFixed é uma Função para encurtar um numero para ter quantas casas decimais se deseja

